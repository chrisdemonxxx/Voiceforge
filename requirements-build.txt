# VoiceForge API - Build Prerequisites for vLLM CUDA Compilation
# These must be installed BEFORE requirements-deployment.txt

# Core build tools (needed for vLLM CUDA kernel compilation)
pip>=23.0
setuptools>=68.0
setuptools-scm
wheel
packaging>=23.0

# NumPy (required before vLLM - Python 3.11 allows 1.24.3 which resolves TTS/librosa conflicts)
numpy==1.24.3

# Build system
ninja
cmake

# Numba (also needs to be pre-installed for librosa/vllm)
numba==0.58.1
